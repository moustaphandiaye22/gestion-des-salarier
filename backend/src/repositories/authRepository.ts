// Auth repository - placeholder for future refresh token storage
// Currently using stateless JWT, so no database operations needed

export class AuthRepository {
  // Placeholder methods for future implementation if refresh tokens need to be stored
  // async storeRefreshToken(token: string, userId: number): Promise<void> {}
  // async findRefreshToken(token: string): Promise<any> {}
  // async revokeRefreshToken(token: string): Promise<void> {}
}
